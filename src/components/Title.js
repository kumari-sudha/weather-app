import React from "react";

const Title = () => (
  <div>
    <h1 className="weather-title">Weather Finder</h1>
		<h3 className="weather-sub-title">Find out temperature, conditions and more...</h3>
  </div>
);

export default Title;
